#include<stdio.h>

int main()
{
    int id = 141;
    int id2 = 142;

    int sum = id + id2;
    printf("The sum of %d and %d is %d\n", id, id2, sum);

    int diff = id - id2;
    printf("The difference of %d and %d is %d\n", id, id2, diff);

    return 0;
}